services:
  - type: web
    name: uploaddocument-web
    env: node
    buildCommand: npm install && npm run build
    startCommand: npx serve -s dist -l 10000
    envVars:
      - key: VITE_API_BASE_URL
        value: https://uploaddocumentbe.onrender.com/api
      - key: PORT
        value: 10000